# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/edc_management
spring.datasource.username=edc_user
spring.datasource.password=edc_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Security Configuration - FIXED
# Option 1: Use plain text with NoOpPasswordEncoder (Development)
spring.security.user.name=admin
spring.security.user.password=admin123
spring.security.user.roles=ADMIN

# Option 2: Use BCrypt encoded password (Production)
# Generate BCrypt password using: https://bcrypt-generator.com/
# Or use: BCryptPasswordEncoder encoder = new BCryptPasswordEncoder(); encoder.encode("admin123");
# spring.security.user.password=$2a$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2uheWG/igi.

# Application Properties
app.hmac.secret=EDCmgmt2025!.?

# Enable test endpoints for development
test.endpoints.enabled=true

# Logging Configuration
logging.level.com.example.edcmanagement=DEBUG
logging.level.org.springframework.security=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n